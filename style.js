// Costanti per la sala cinematografica
const M = 5; // numero di file
const N = 10; // numero di colonne

// Variabili globali
let prenotazioni = []; // array di oggetti prenotazione
let salaEl = document.getElementById("sala"); // elemento DOM della sala cinematografica
let prenotazioneFormEl = document.getElementById("prenotazione-form"); // elemento DOM del form di prenotazione
let cancellazioneFormEl = document.getElementById("cancellazione-form"); // elemento DOM del form di cancellazione
let listaPrenotazioniEl = document.getElementById("lista-prenotazioni"); // elemento DOM della lista di prenotazioni
let salvaPrenotazioniEl = document.getElementById("salva-prenotazioni"); // elemento DOM del bottone salva prenotazioni
let caricaPrenotazioniEl = document.getElementById("carica-prenotazioni"); // elemento DOM del bottone carica prenotazioni

/* Funzione per generare la sala cinematografica
function generaSalaCinematografica() {
     // Genera il layout della sala cinematografica
     let html = "";
     for (let i = 0; i < M; i++) 
         html += "<div class='fila'>";
         for (let j = 0; j < N; j++) 
             html += "<button class='poltrona' data-fila='" + String.fromCharCode (i +);
 
}*/
   
